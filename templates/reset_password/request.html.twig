{% extends 'base.html.twig' %}

{% block title %}{{ 'front.forgotpassword.title'|trans }}{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
    <h1>{{ 'front.forgotpassword.title'|trans }}</h1>

    {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}
        <div>
            <small>
                {{ 'front.forgotpassword.requestExplanation'|trans }}
            </small>
        </div>

        <button class="btn btn-primary">{{ 'front.forgotpassword.sendPasswordResetEmail'|trans }}</button>
    {{ form_end(requestForm) }}
{% endblock %}
