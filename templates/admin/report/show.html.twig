{% extends 'admin/base.html.twig' %}

{% block title %}Report{% endblock %}

{% block body %}
    <h1>Report</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ report.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ report.type }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ report.localisation }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ report.city }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ report.address }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ report.description }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ report.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ report.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ report.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ report.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_report_index') }}">back to list</a>

    <a href="{{ path('app_admin_report_edit', {'id': report.id}) }}">edit</a>

    {{ include('admin/report/_delete_form.html.twig') }}
{% endblock %}
